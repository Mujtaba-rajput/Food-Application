import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    justifyContent: 'center',
    alignItems: 'center',
  },
  indicator: {
    flexWrap: 'wrap',
    justifyContent: 'center',
    alignItems: 'center',
  },
});
